# Supabase (painel do projeto -> Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key

# Groq (para o pipeline de analise do PDF, fase seguinte)
GROQ_API_KEY=sua-groq-key

# Worker do WhatsApp (o server.js que ja esta rodando)
WA_WORKER_URL=http://localhost:3001
WA_WORKER_TOKEN=92fb97fc...  # o mesmo AUTH_TOKEN do worker
